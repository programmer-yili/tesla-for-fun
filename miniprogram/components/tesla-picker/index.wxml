<picker class="picker" bindchange="onChange" value="{{ index }}" range="{{options}}">
    <tesla-input writable="{{ false }}" required label="{{ label }}" value="{{ value }}"/>
    <image class="icon-down" src="../../images/ic_arrow_down.svg" />
</picker>