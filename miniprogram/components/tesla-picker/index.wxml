<picker class="picker" bindchange="onChange" value="{{ index }}" range="{{ optionList }}">
    <tesla-input writable="{{ false }}" required label="{{ title }}" value="{{ value }}"/>
    <image class="icon-down" src="../../images/ic_arrow_down.svg" />
</picker>